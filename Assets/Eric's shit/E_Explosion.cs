using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class E_Explosion : MonoBehaviour
{/*
    [SerializeField] int explosionDamage;
    
    private void OnTriggerEnter(Collider other)
    {
        var tryPlayer = other.transform.gameObject.GetComponent<PlayerHealth>();
        if (tryPlayer != null)
        {
            tryPlayer.TakeDamage(explosionDamage);
        }

        var tryEnemy = other.transform.gameObject.GetComponent<DummyEnemy>();
        if (tryEnemy != null)
        {
            tryEnemy.TakeDamage(explosionDamage);
        }
    }*/
}
